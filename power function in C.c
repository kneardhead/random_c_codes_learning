#include<stdio.h>
#include<math.h>

int main () {
        double x,z,r; // we are to define x^z
        printf("\n Put the numbers for calculation of the Power.");
        printf("\n The base:");
        scanf("%lf",&x);
        printf("\n The exponent power);");
        scanf("%lf",&z);


        r = pow(x,z);
        printf("\n The number is %lf",r);











}

